{
  "name": "setter-app",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "turbo run dev --filter=@setter/web",
    "dev:mobile": "turbo run dev --filter=@setter/mobile",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "postinstall": "cp -r apps/mobile/node_modules/expo node_modules/expo 2>/dev/null || true"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "npm@10.2.0",
  "type": "commonjs"
}
